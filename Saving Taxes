#include <stdio.h>

int main(void) {
	// your code goes here
	int T;
	scanf("%d",&T);
	for(int i=0;i<T;i++)
	{
	    int x;
	    scanf("%d",&x);
	    int y;
	    scanf("%d",&y);
	    if(x>y)
	    {
	        int invest= x-y;
	        printf("%d\n",invest);
	    }
	}
}


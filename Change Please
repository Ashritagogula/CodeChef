#include <stdio.h>

int main() {
    int T; 
    scanf("%d", &T); 
int i;
    // Process each test case
    for (i = 0; i < T; i++) {
        int X;
        scanf("%d", &X); 
        int change = 100 - X;
        int maxAmount = change - (change % 10);

        printf("%d\n", maxAmount);
    }

    return 0;
}

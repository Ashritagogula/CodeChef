#include <stdio.h>

int main(void) {
		int t;
	scanf("%d",&t);
	for(int i=0;i<t;i++)
	{
	    int x,y;
	    scanf("%d%d",&x,&y);
	    if(x<=y) //students <= no of chairs
	    {
	        printf("0\n");
	    }
	    else
	    {
	        printf("%d\n",x-y);
	    }
	}

}

t = int(input())
for _ in range(t):
    n, m = map(int, input().split())
    if n % m == 0 and (n // m) % 2 == 0:  # check equal division and even per friend
        print("Yes")
    else:
        print("No")
